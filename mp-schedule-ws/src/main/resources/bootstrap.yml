spring:
  application:
    name: mp-schedule-local
  cloud:
     config:
        uri: https://ws-config.cnova.com.br
        label: mp-local
        enabled: false

eureka:
  instance:
    appname: ${spring.application.name}       